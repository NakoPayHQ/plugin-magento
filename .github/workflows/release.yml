name: Release plugin-magento

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No internal URLs
        run: bash ./scripts/check-no-internal-urls.sh ./
      - name: Build zip
        run: |
          cd .
          zip -r ../magento.zip magento -x '*/node_modules/*' '*/vendor/*' '*/.git/*'
      - name: GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: magento.zip
          generate_release_notes: true
          name: Magento 2 ${{ github.ref_name }}
